export function calculateCurrentStreakDays(firstUrgeTimestamp: number | null, latestRelapseTimestamp: number | null, now?: number): number;
export function calculatePledgeStreak(timestamps: number[], now?: number): number;
